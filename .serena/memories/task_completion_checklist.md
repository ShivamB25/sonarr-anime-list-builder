After code changes, determine and run relevant validators before finishing. From package.json, key commands are `bunx vite build && bunx wrangler deploy --dry-run` for build validation and Wrangler/D1 commands for local/remote DB migration workflows. There are currently no dedicated lint or test scripts defined in package.json, so build validation is the primary available project-wide check unless the task requires narrower runtime verification. Before commits, review `git diff --cached` and `git status` for scope and secrets.