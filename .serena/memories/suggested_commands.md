Development commands: `bunx wrangler dev` (full app dev server), `bunx vite build --watch` (client rebuild loop), `bun run build` (package build validation), `bun run deploy` (package deploy). Database commands: `bun run db:generate`, `bun run db:migrate:local`, `bun run db:migrate:remote`, `bun run db:studio`. Useful Darwin shell commands available: `git`, `ls`, `pwd`, `rg`, `curl`, `wget`, `python3`, `ffmpeg`. Repo notes: wrangler.toml main entry is `src/server/index.ts`, static assets come from `dist/client`, scheduled cron triggers are configured, and the custom domain is `airing.aniex.site`.
