Development commands: `bunx wrangler dev` (full app dev server), `bunx vite build --watch` (client rebuild loop), `bunx vite build && bunx wrangler deploy --dry-run` (build validation), `bunx vite build && bunx wrangler deploy` (deploy). Database commands: `bunx drizzle-kit generate`, `bunx wrangler d1 migrations apply airing-list-db --local`, `bunx wrangler d1 migrations apply airing-list-db --remote`, `bunx drizzle-kit studio`. Useful Darwin shell commands available: `git`, `ls`, `pwd`, `rg`, `curl`, `wget`, `python3`, `ffmpeg`. Repo notes: wrangler.toml main entry is `src/server/index.ts`, static assets come from `dist/client`, and scheduled cron triggers are configured.